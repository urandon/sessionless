bucket = "replace-with-bootstrap-output-state-bucket-name"
key    = "bootstrap/terraform.tfstate"

# Credentials are intentionally absent. Select a restricted external AWS
# credentials file containing a short-lived key with AWS_SHARED_CREDENTIALS_FILE
# and AWS_PROFILE.
