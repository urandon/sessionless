bucket = "replace-with-bootstrap-state-bucket"
key    = "sessionless/cloud-dev/terraform.tfstate"

# Credentials are read from the external AWS credentials profile selected by
# AWS_SHARED_CREDENTIALS_FILE and AWS_PROFILE.
